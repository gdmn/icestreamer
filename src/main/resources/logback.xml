<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <logger name="pl.devsite" level="info"/>
    <logger name="pl.devsite.icestreamer" level="all"/>

    <root level="error">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
